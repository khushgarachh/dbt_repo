seeds:
  - name: zipcodes
    config:
      column_types:
        zipcodes: varchar
  - name: zipcodes2
    config:
      column_types:
        zipcodes: varchar
      delimiter: "|"
  - name: zipcodes3
    config:
      quote_columns: true
      column_types:
        country code: varchar
        country name: varchar
      delimiter: "|"
